# Postgres connection string consumed by drizzle-kit (src/client.ts reads the
# same variable at runtime). Any Postgres works — local docker, Supabase, RDS,
# Neon, etc. Supabase's "connection string" is a plain Postgres URL.
DATABASE_URL=postgres://brandfactory:brandfactory@localhost:5432/brandfactory
